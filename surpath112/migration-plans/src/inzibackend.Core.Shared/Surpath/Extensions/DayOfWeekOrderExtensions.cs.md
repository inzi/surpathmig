# New
## Filename
DayOfWeekOrderExtensions.cs
## Relative Path
inzibackend.Core.Shared\Surpath\Extensions\DayOfWeekOrderExtensions.cs
## Language
C#
## Summary
The provided C# code defines a static class `DayOfWeekOrderExtensions` with three methods:
1. `GetDaysStartingWithMonday()` which yields all days starting from Monday to Sunday.
2. `GetDaysStartingWith(DayOfWeek startDay)` which loops through seven days starting from the specified day using modulo 7 arithmetic.
3. `GetDayOfWeekAbbr(DayOfWeek day)` which returns a string abbreviation for each day of the week (e.g., 'M' for Monday).
The class provides utility methods for working with days of the week, likely used in an ASP.NET Zero application for date-time operations.
## Changes
New file
## Purpose
Utility class providing methods to work with days of the week, useful for date-time operations in an ASP.NET Zero application.
