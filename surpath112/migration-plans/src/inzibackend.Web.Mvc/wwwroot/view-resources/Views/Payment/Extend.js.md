# Modified
## Filename
Extend.js
## Relative Path
inzibackend.Web.Mvc\wwwroot\view-resources\Views\Payment\Extend.js
## Language
JavaScript
## Summary
The modified file includes an additional function `init` within the IIFE which calls `handleRegister()` and triggers a checkout process. The unmodified version lacks this `init` function.
## Changes
Added `init` function that handles registration and initiates the checkout process by calling `handleRegister()`.
## Purpose
The file implements click handler for checkout, enabling form submission upon payment period selection.
