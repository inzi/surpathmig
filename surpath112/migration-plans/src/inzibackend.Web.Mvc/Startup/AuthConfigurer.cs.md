# Modified
## Filename
AuthConfigurer.cs
## Relative Path
inzibackend.Web.Mvc\Startup\AuthConfigurer.cs
## Language
C#
## Summary
Both files contain configuration code for authentication settings in an ASP.NET Zero application. They enable various social login providers such as OpenId Connect, Microsoft Account, Google, Twitter, Facebook, and WsFederation, each with their own configuration options.
## Changes
The modified content includes a formatting change by adding an extra space after `OpenIdConnectOptions` in the `IOptionsMonitor` type declaration. This is likely for code consistency or readability.
## Purpose
These files configure the authentication system to support multiple social login providers with customizable settings per provider.
