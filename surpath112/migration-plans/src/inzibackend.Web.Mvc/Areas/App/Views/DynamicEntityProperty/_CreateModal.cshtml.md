# Modified
## Filename
_CreateModal.cshtml
## Relative Path
inzibackend.Web.Mvc\Areas\App\Views\DynamicEntityProperty\_CreateModal.cshtml
## Language
Unknown
## Summary
The modified file contains code for a modal header where dynamic properties are selected and assigned to an entity. The unmodified version is identical except for using .tsx instead of .cshtml in one partial async call.
## Changes
The only change is the file extension in one Html.PartialAsync call, changing from .cshtml to .tsx.
## Purpose
This file is part of an ASP.NET application handling dynamic properties and uses partial async for rendering parts of a modal.
