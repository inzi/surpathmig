# Modified
## Filename
Default.cshtml
## Relative Path
inzibackend.Web.Mvc\Areas\App\Views\Shared\Themes\Theme5\Components\AppTheme5Brand\Default.cshtml
## Language
Unknown
## Summary
The modified file contains styling configurations for an AppLogo component, with two different implementations based on dark mode settings.
## Changes
Added a new line `var theme = await GetTheme();` and modified two lines in the unmodified content. The first modification changes `dark` to `dark-sm`, while the second modification changes `light` to `light-sm`.
## Purpose
The file is part of an ASP.NET Zero application, likely used for styling components based on dark mode preferences.
