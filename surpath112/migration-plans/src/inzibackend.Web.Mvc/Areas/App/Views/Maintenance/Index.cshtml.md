# Modified
## Filename
Index.cshtml
## Relative Path
inzibackend.Web.Mvc\Areas\App\Views\Maintenance\Index.cshtml
## Language
Unknown
## Summary
The modified file contains an ASP.NET Web App Maintenance View with a header and two tabs (Caches and WebSiteLogs). Each tab includes buttons to manage cache data. The unmodified version uses standard HTML attributes for styling and scripts.
## Changes
The modifications include adding the `asp-append-version` attribute in the styles section, changing from `<script src>` to `<script abp-src>`, and maintaining similar functionality with navigation tabs and buttons.
## Purpose
To implement a user interface for cache management within an ASP.NET application.
