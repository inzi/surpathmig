# New
## Filename
Hospital.cs
## Relative Path
inzibackend.Core\Surpath\Hospital.cs
## Language
C#
## Summary
The provided C# code defines a `Hospital` class in the `inzibackend.Surpath` namespace. It inherits from `FullAuditedEntity` and implements `IMayHaveTenant`, indicating it's part of domain-driven design for data access layer integration. The class contains several required properties such as `Name`, `PrimaryContact`, `Address1`, etc., which are essential for identifying and managing hospital data. The class uses auditing annotations to ensure consistency across the application.
## Changes
New file
## Purpose
The file represents a core domain entity in an ASP.NET Zero solution, providing structured data model support with auditing capabilities.
