# Custom Changes: surpath112 vs aspnetzeromvc11.4 (for porting to surpath200)\n\n## Directory Structure Overview\n- **aspnetzeromvc11.4/**: Clean ASP.NET Zero MVC 11.4 template (~1225 .cs files in `inzibackend/src`).\n  - Root: `aspnetzeromvc11.4.sln`, `inzibackend/` dir.\n- **surpath112/**: Extended/customized version with **custom top-level files & dirs** (20k+ total files).\n  - Contains `aspnetzeromvc11.4/inzibackend/` (appears to be a copy of the template).\n  - Custom additions: `AspNetZeroRadTool/`, `build/`, `.claude/`, `docker/`, migration docs (e.g., `IsEnabled_Analysis.md`, `TenantSurpathService_IsEnabled_Implementation_Plan.md`), `src/` (additional?), `sql/`, `test/`, `ui-tests/`, etc.\n- **Core project dirs match**: Both have `inzibackend/src/[Application|Core|Web.*|...]` with identical .cs count (1225 each).\n\n## Key Custom Changes in surpath112 (to port to surpath200)\nsurpath112 = aspnetzeromvc11.4 **+** these customizations:\n\n### 1. **Solution Structure Changes**\n```\ndiff surpath112/inzibackend.Web.sln aspnetzeromvc11.4/aspnetzeromvc11.4.sln\n```\n- **surpath112/inzibackend.Web.sln** (custom top-level SLN):\n  | Change | Description |\n  |--------|-------------|\n  | Added \"Solution Items\" section | Includes `.gitignore`, `common.props`, GitHub workflows (CI/CD), `FEATURENOTES.md`, `SurpathServices.md`, etc. |\n  | Custom project GUIDs & ordering | Reordered projects; added `inzibackend.SurpathSeedHelper`, `ConsoleClient`. |\n  | VS Version diff | 17.0 → 17.5. |\n- **Action for surpath200**: Copy custom SLN. Merge solution items (props, workflows). Update GUIDs if needed.\n\n### 2. **Top-Level Custom Files/Dirs (Surpath-specific)**\n```\nsurpath112/ (not in aspnetzeromvc11.4 root):\n├── AspNetZeroRadTool/          # Custom RAD tool scaffolding\n├── CLAUDE.md, Compliance_*.md  # AI-assisted migration notes/plans\n├── docker/                     # Docker configs (compose files)\n├── sql/                        # Custom SQL scripts (e.g., fix-pricing-override-enabled.sql)\n├── src/                        # ? Additional src (check for custom modules)\n├── test/, ui-tests/            # Extended tests\n├── TenantSurpathService_IsEnabled_Implementation_Plan.md  # Business logic plans\n├── IsEnabled_Analysis.md       # Feature flag analysis\n├── swagger.json                # Custom API docs\n├── vsusertransfer.md           # User migration notes\n└── ... (see full ls in logs)\n```\n- **custom-services.txt** (in mig root): Likely lists Surpath custom services.\n\n### 3. **Backend Customizations (inzibackend/)**\n- **Structure near-identical** except minor `.DS_Store`.\n- **Custom code likely in**:\n  | Area | Files to diff |\n  |------|--------------|\n  | Services | `src/*/Surpath*Service.cs`, `Application/Services/` |\n  | Entities | `Core/Entities/Surpath*.cs` |\n  | Modules | Custom ABP modules in `src/*/Modules/` |\n  | Seed data | `SurpathSeedHelper.cs`, `Migrator/` |\n- **~0 code diffs found in shallow check** → Deep diff needed for custom impl (e.g., `IsEnabled` logic).\n\n### 4. **surpath200 Status**\n- Has `aspnet-core/`, `react/` → .NET Core + React rewrite (not MVC).\n- **Porting plan**: \n  1. Copy custom services/entities from `surpath112/inzibackend/src/` → `surpath200/aspnet-core/`.\n  2. Adapt MVC → Razor Pages/Controllers → Minimal APIs/Endpoints.\n  3. Migrate custom SQL/scripts.\n  4. Port tests/UI logic to React.\n- Reference `migration/` guides (permission mapping, jQuery→React, DataTables, modals).\n\n## Next Steps\n- **Deep diff**: `diff -r surpath112/aspnetzeromvc11.4/inzibackend/src aspnetzeromvc11.4/inzibackend/src` (selective files).\n- **Custom services list**: Review `custom-services.txt`.\n- **Validate ports**: Check `surpath200` for existing merges.\n\n**Generated**: 2026-02-04 by baseline-diffs subagent.